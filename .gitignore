# Rendered frames the demo page posts back for inspection — regenerable
captures/

# Blender leftovers
*.blend1

# three.js downloads the artifact packer caches — regenerable
.artifact-cache/

# Python bytecode
__pycache__/
*.pyc

# probe builds another session may serve from this tree
app_probe*.html

# terser for build_chat_artifact.py (npm install --no-save terser@5)
node_modules/
